# === Required Configuration ===

# MongoDB connection string for audit logging
# Format: mongodb+srv://username:password@cluster.mongodb.net/
# You can use either MONGO_URI or MONGODB_URI
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/

# GCS bucket containing parquet data
GCS_BUCKET_NAME=peskas-data-bucket

# === Authentication Configuration ===

# Path to API keys configuration file (YAML)
# Copy api_keys.yaml.example to api_keys.yaml and customize
API_KEYS_CONFIG_PATH=api_keys.yaml

# DEPRECATED: Legacy single API key (use api_keys.yaml instead)
# API_SECRET_KEY=your-secret-key-here

# === Optional Configuration ===

# MongoDB database and collection names for audit logs
# MONGODB_DATABASE=api-logs
# MONGODB_AUDIT_COLLECTION=logs

# Debug mode (enables CORS for all origins)
DEBUG=false

# GCS project ID (uses default credentials if not set)
# GCS_PROJECT_ID=your-gcp-project

# API settings
# API_PREFIX=/api/v1
# API_TITLE=Peskas Fishery Data API

# Schema settings
# DEFAULT_DATE_COLUMN=landing_date
# DEFAULT_STATUS=validated
# GCS_PATH_TEMPLATE={country}/{status}/
# GCS_FILENAME_PATTERN=trips-\w+__(\d{14})_[a-f0-9]+__.parquet

# Query limits
# MAX_ROWS_DEFAULT=100000
# MAX_ROWS_LIMIT=1000000
